import { StyleSheet } from 'react-native'
import { SIZES } from '../constants'

export const stSwitch = StyleSheet.create({
  container: {
    alignItems: 'flex-end',
    justifyContent: 'center',
    padding: SIZES.xSmall
  }
})
