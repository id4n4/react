import { StyleSheet } from 'react-native'
import { FONTS, SIZES } from '../constants'

export const stHeader = StyleSheet.create({
  title: {
    fontFamily: FONTS.magro,
    fontSize: SIZES.xxLarge
  }
})
